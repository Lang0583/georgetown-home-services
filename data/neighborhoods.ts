/**
 * Neighborhood area metadata (service×area landings retired — see `lib/neighborhood-redirects.ts`).
 */

export {
  NEIGHBORHOOD_AREAS,
  NEIGHBORHOOD_AREA_SLUGS,
  NEIGHBORHOOD_SERVICE_SLUGS,
  type NeighborhoodAreaSlug,
  type NeighborhoodServiceSlug,
} from "@/lib/neighborhood-redirects";
