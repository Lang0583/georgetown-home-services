/**
 * Site CTAs — use `CTASection` for boxed blocks, `SiteCTAButtons` for inline rows.
 * Labels are fixed in `lib/site-cta.ts` (View Top Providers / Email Me Top Georgetown Providers).
 */
export { default as CTASection, SiteCTAButtons } from "./CTASection";
export { CTA_EMAIL_PROVIDERS, CTA_VIEW_TOP_PROVIDERS } from "../lib/site-cta";
